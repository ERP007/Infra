name: msa-server

services:
  cloudflared:
    image: cloudflare/cloudflared:2026.5.1
    user: "${CLOUDFLARED_UID:-1000}:${CLOUDFLARED_GID:-1000}"
    restart: unless-stopped
    command:
      - tunnel
      - --config
      - /etc/cloudflared/config.yml
      - run
    volumes:
      - ./server-secrets/cloudflared:/etc/cloudflared:ro
    extra_hosts:
      - host.docker.internal:host-gateway
    depends_on:
      - nginx
    networks:
      - edge-app
      - edge-ci

  nginx:
    image: nginx:1.27-alpine
    restart: unless-stopped
    depends_on:
      - gateway-service
    ports:
      - "127.0.0.1:80:80"
    volumes:
      - ./nginx/nginx.server.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - edge-app
      - app

  gateway-service:
    build:
      context: ../gateway-service
      dockerfile: Dockerfile
    image: erp007/gateway-service:server
    restart: unless-stopped
    env_file:
      - ./server-secrets/gateway-service.env
    depends_on:
      user-service:
        condition: service_started
      item-service:
        condition: service_started
      inventory-service:
        condition: service_started
      procurement-service:
        condition: service_started
      sales-service:
        condition: service_started
    networks:
      - app
      - service

  user-service:
    build:
      context: ../user-service
      dockerfile: Dockerfile
    image: erp007/user-service:server
    restart: unless-stopped
    env_file:
      - ./server-secrets/user-service.env
      - ./server-secrets/redis.env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - service
      - data

  item-service:
    build:
      context: ../item-service
      dockerfile: Dockerfile
    image: erp007/item-service:server
    restart: unless-stopped
    env_file:
      - ./server-secrets/item-service.env
      - ./server-secrets/redis.env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - service
      - data

  inventory-service:
    build:
      context: ../inventory-service
      dockerfile: Dockerfile
    image: erp007/inventory-service:server
    restart: unless-stopped
    env_file:
      - ./server-secrets/inventory-service.env
      - ./server-secrets/redis.env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - service
      - data

  procurement-service:
    build:
      context: ../procurement-service
      dockerfile: Dockerfile
    image: erp007/procurement-service:server
    restart: unless-stopped
    env_file:
      - ./server-secrets/procurement-service.env
      - ./server-secrets/redis.env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - service
      - data

  sales-service:
    build:
      context: ../sales-service
      dockerfile: Dockerfile
    image: erp007/sales-service:server
    restart: unless-stopped
    env_file:
      - ./server-secrets/sales-service.env
      - ./server-secrets/redis.env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - service
      - data

  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    env_file:
      - ./server-secrets/postgres.env
    ports:
      - "127.0.0.1:15432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./postgres/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$${POSTGRES_USER}\" -d \"$${POSTGRES_DB}\""]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - data

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    env_file:
      - ./server-secrets/redis.env
    ports:
      - "127.0.0.1:16379:6379"
    command:
      - sh
      - -c
      - |
        if [ -n "$${REDIS_PASSWORD:-}" ]; then
          exec redis-server --appendonly yes --requirepass "$${REDIS_PASSWORD}"
        fi
        exec redis-server --appendonly yes
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD-SHELL", "if [ -n \"$${REDIS_PASSWORD:-}\" ]; then redis-cli -a \"$${REDIS_PASSWORD}\" ping; else redis-cli ping; fi"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - data

networks:
  edge-app:
    name: msa-edge-app
  edge-ci:
    name: msa-edge-ci
  app:
    name: msa-app
  service:
    name: msa-service
  data:
    name: msa-data

volumes:
  postgres_data:
  redis_data:
